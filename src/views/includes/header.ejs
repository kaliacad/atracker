<body>
    <nav class="navbar navbar-expand-lg bg-danger text-light px-5 fixed-top">
        <div class="container mx-5">
            <a class="navbar-brand text-light" href="/">Attendacy GDA</a>
            <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation"
            >
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul
                class="navbar-nav text-light me-auto mb-2 mb-lg-0 d-flex justify-content-end w-100"
                >
                <% if(userId) { %>
                <li class="nav-item">
                    <a
                    class="nav-link text-light"
                    aria-current="page"
                    href="/admin"
                    >Dashboard</a
                    >
                </li>
                <li class="nav-item">
                    <a
                    class="nav-link text-light"
                    aria-current="page"
                    href="/admin/students"
                    >Apprenants</a
                    >
                </li>
                <li class="nav-item">
                    <a class="nav-link mr-5 text-light" href="/admin/add-presence">Presence</a>
                </li>
                <form class="nav-item" method="POST" action="/logout">
                    <button
                    class="nav-link p-1 text-sm text-light h-100 btn btn-outline-secondary"
                    type="submit"
                    >
                    Se deconnecter
                    </button>
                </form>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link text-center text-light mr-5" href="/login">Se connecter</a>
                </li>
                <% } %>
                </ul>
            </div>
        </div>
    </nav>
